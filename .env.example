# PostgreSQL 連線字串（Zeabur 建立 PostgreSQL 服務後提供）
DATABASE_URL="postgresql://user:password@host:5432/hometool"

# 管理 API 密鑰：n8n 呼叫 /api/admin/* 時帶 Authorization: Bearer <ADMIN_TOKEN>
# 產生方式：openssl rand -hex 32
ADMIN_TOKEN="change-me-to-a-long-random-string"
